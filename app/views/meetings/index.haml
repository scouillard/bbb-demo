=console
%p#notice= notice
%h1 Meetings


- if Meeting.all.empty? && current_user == nil
  = "There are no meetings. Please sign in to create a meeting."

- if Meeting.all.empty? && current_user != nil
  %br
  = link_to 'New Meeting', new_meeting_path

- else
  %table
    %thead
      %tr
        %th Meeting Name
        %th Welcome Message
        %th Is the meeting being recorded?
        %th ID
        %th{:colspan => "3"}
    %tbody
      - @meetings.each do |meeting|
        %tr
          %td= meeting.name
          %td= meeting.welcome_message
          %td= meeting.meeting_record
          %td= meeting.external_meeting_id

          %td= link_to 'Destroy', meeting, method: :delete, data: { confirm: 'Are you sure?' }
  %br/

= link_to 'New Meeting', new_meeting_path
%br
= link_to 'View Recordings', recordings_path


-# = render :partial => 'recordings/index', locals: { recordings: @recordings }
